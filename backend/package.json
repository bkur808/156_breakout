{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "socket.io": "^4.8.1"
  },
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd ../frontend && npm install && npm run build"
  }
}
